<%= form_for([@document, @document.comments.new]) do |f|  %>
	<div class="field">
		<%= f.text_area :comment, :rows=>5 %>
	</div>
	<div class="actions">
		<%= f.submit 'Add', :class=>"span1" %>
	</div>
<%end%>
